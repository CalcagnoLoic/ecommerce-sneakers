import { Canvas, Meta } from '@storybook/blocks';

import * as IconContentStories from './index.stories';

<Meta of={IconContentStories} />

# Icon content

A icon content is a component that display the source of an image

<Canvas of={IconContentStories.PlusIcon} />
<Canvas of={IconContentStories.MinusIcon} />
<Canvas of={IconContentStories.CartIcon} />
<Canvas of={IconContentStories.LogoIcon} />
<Canvas of={IconContentStories.CloseMenuIcon} />
<Canvas of={IconContentStories.MenuIcon} />
