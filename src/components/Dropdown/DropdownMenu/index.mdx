import { Canvas, Meta } from '@storybook/blocks';

import * as DropdownStories from './index.stories';

<Meta of={DropdownStories} />

# Dropdown Menu

A Dropdown menu is a component that can be clicked on hamburger to display a lateral sidebar.

**Warning**, decrease the size to see the hamburger icon ;)

<Canvas of={DropdownStories.Default} />
