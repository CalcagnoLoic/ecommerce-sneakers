import { Canvas, Meta } from '@storybook/blocks';

import * as BasketStories from './index.stories';

<Meta of={BasketStories} />

# Basket

A basket is an icon that is can be clicked to open the basket

<Canvas of={BasketStories.Default} />