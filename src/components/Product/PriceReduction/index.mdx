import { Canvas, Meta } from "@storybook/blocks";

import * as PriceReductionStories from "./index.stories";

<Meta of={PriceReductionStories} />

# Price reduction

The PriceReduction is the component that display the wrapper and the amount of reduction for a product

<Canvas of={PriceReductionStories.Default} />
