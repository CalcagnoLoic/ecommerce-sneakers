import { Canvas, Meta } from "@storybook/blocks";

import * as ProductReductionStories from "./index.stories";

<Meta of={ProductReductionStories} />

# Product reduction

The ProductReduction is the component that display the wrapper and the amount of reduction for a product

<Canvas of={ProductReductionStories.Default} />
